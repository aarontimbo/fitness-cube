security {

	// see DefaultSecurityConfig.groovy for all settable/overridable properties

	active = true

	loginUserDomainClass = "SecUser"
	authorityDomainClass = "SecRole"
	requestMapClass = "Request"
}
